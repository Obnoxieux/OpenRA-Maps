Player:
	-ConquestVictoryConditions:
	MissionObjectives:
		Cooperative: True
	Shroud:
		FogLocked: True
		FogEnabled: False
		ExploredMapLocked: True
		ExploredMapEnabled: False
	PlayerResources:
		DefaultCashLocked: True
		DefaultCash: 5000

World:
	MissionData:
		BriefingVideo: ally10.vqa
		LossVideo: trinity.vqa
		StartVideo: mcv_land.vqa
		Briefing: Kosygin has indicated that this is the sight of Stalin's main atomic weapons plant. Use extreme care in approaching the Soviet base -- we don't know if any atomic bombs are armed yet. \n\nTake the facility off-line and then destroy any atomic weapons that exist.\n\nIf the Soviets should somehow manage to launch their missiles, you must capture their command centre as soon as possible.
	MapBuildRadius:
		AllyBuildRadiusLocked: True
		AllyBuildRadiusEnabled: True
APC:
	Buildable:
		Prerequisites: ~vehicles.allies
HIND:
	Buildable:
		Prerequisites: ~aircraft.soviet
HPAD:
	Buildable:
		Prerequisites: dome, ~techlevel.medium
SHOK:
	Buildable:
		Prerequisites: stek, tsla, ~infantry.soviet
#	E3:
#		Buildable:
#			Prerequisites: ~infantry.allies
HELI:
	Buildable:
		Prerequisites: ~vehicles.allies
TRUK:
	Buildable:
		Prerequisites: ~disabled
	-DeliversCash:
	SpawnActorOnDeath:
		Actor: scrate
SCRATE:
	Inherits: ^Crate
	Tooltip:
		Name: Nuclear Bomb
	ExplodeCrateAction:
		Weapon: Atomic
		SelectionShares: 1
MGG:
	Buildable:
		Prerequisites: atek, ~vehicles.allies
TTNK:
	Buildable:
		Prerequisites: stek, tsla, ~vehicles.soviet
FTRK:
	Buildable:
		Prerequisites: ~disabled
DTRK:
	Buildable:
		Prerequisites: stek, ~vehicles.soviet
CTNK:
	Buildable:
		Prerequisites: atek, ~vehicles.allies
STNK:
	Buildable:
		Prerequisites: atek, ~vehicles.allies
AFLD:
	AirstrikePower@parabombs:
		OrderName: UkraineParabombs
		Prerequisites: aircraft.soviet
		Icon: parabombs
		ChargeTime: 300
		Description: Parabombs
		LongDesc: A squad of Badgers drops parachuted\nbombs on your target.
		SelectTargetSpeechNotification: SelectTarget
		CameraActor: camera
		CameraRemoveDelay: 150
		UnitType: badr.bomber
		QuantizedFacings: 8
		DisplayBeacon: true
		BeaconPoster: pbmbicon
		SquadSize: 3
		SquadOffset: 1792,1792,0
		ArrowSequence: arrow
		ClockSequence: clock
		CircleSequence: circles
BADR:
	Health:
		HP: 45000
MSLO:
	Buildable:
		Prerequisites: ~disabled
PDOX:
	Buildable:
		Prerequisites: ~disabled
SYRD:
	Buildable:
		Prerequisites: ~disabled
V01:
	SpawnActorOnDeath:
		Actor: moneycrate
Zombie:
	Buildable:
		Prerequisites: ~disabled
Ant:
	Buildable:
		Prerequisites: ~disabled
